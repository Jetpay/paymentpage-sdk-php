{
    "name": "jetpay/paymentpage-sdk",
    "description": "Jetpay SDK",
    "type": "library",
    "authors": [
        {
            "name": "mr. SDK",
            "email": "sdk@jetpay.kz"
        }
    ],
    "autoload": {
        "psr-4": { "jetpay\\": "src/" }
    },
    "require": {
    },
    "require-dev": {
        "phpunit/phpunit": "^6.5||^7.2",
        "squizlabs/php_codesniffer": "^3.3"
    },
    "scripts": {
        "lint": "phpcs --report=full --report-width=120",
        "test": "phpunit --coverage-clover clover.xml"
    }
}
